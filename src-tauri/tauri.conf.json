{
  "$schema": "https://schema.tauri.app/config/2",
  "productName": "ridge",
  "version": "0.1.56",
  "identifier": "com.tauri-app.ridge",
  "build": {
    "beforeDevCommand": "node packages/ridge-term/build.mjs --dev && node scripts/ensure-teammate-shim.mjs && node scripts/start-vite-dev.mjs",
    "devUrl": "http://127.0.0.1:5173",
    "beforeBuildCommand": "node packages/ridge-term/build.mjs && pnpm run build && pnpm build:remote && node scripts/build-ridge-mcp-sidecar.mjs && node scripts/build-rdg-sidecar.mjs",
    "beforeBundleCommand": "node scripts/build-ridge-mcp-sidecar.mjs && node scripts/build-rdg-sidecar.mjs && node -e \"setTimeout(()=>process.exit(0),3000)\" && cargo build --release --bin tmux --manifest-path src-tauri/Cargo.toml && pnpm build:cli",
    "frontendDist": "../build"
  },
  "app": {
    "withGlobalTauri": true,
    "windows": [],
    "security": {
      "assetProtocol": {
        "enable": true,
        "scope": ["**"]
      }
    }
  },
  "plugins": {
    "deep-link": {
      "desktop": {
        "schemes": ["ridge"]
      }
    }
  },
  "bundle": {
    "active": true,
    "targets": ["nsis", "msi"],
    "externalBin": ["binaries/ridge-mcp", "binaries/rdg"],
    "icon": [
      "icons/32x32.png",
      "icons/128x128.png",
      "icons/128x128@2x.png",
      "icons/icon.icns",
      "icons/icon.ico"
    ],
    "resources": {
      "../ridge.theme": "ridge.theme",
      "../remote-dist": "remote-dist",
      "../static/docs": "static/docs"
    },
    "windows": {
      "nsis": {
        "installerHooks": "installer-hooks.nsh"
      },
      "wix": {
        "fragmentPaths": ["wix/path-env.wxs"],
        "componentRefs": ["RidgePathEnvVar_0_0_2"]
      }
    }
  }
}

