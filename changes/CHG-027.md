---
id: CHG-027
title: Fix teammate delivery status request ownership
type: SEMANTIC_CHANGE
status: COMPLETED
affects:
  - L2-AGENT-COMMUNE-EXACT-BINDING
---

# Fix teammate delivery status request ownership

Preserve the delivery-status RPC contract while constructing its request without
retaining a borrow across ownership transfer. The command must compile and
continue to normalize the `deliveryId` argument to `delivery_id` before passing
the request to the Hub adapter.

Acceptance: `cargo check --manifest-path src-tauri/Cargo.toml --lib` succeeds.
