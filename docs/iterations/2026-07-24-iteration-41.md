# Iteration 41 / AC4-C2 — Terminal link affordance + TUI arbitration

## What shipped
Ctrl/Cmd-hover shows underline + pointer; TUI mouse reporting keeps bare clicks for the program; Ctrl+click opens OSC8/text links; path resolution against pane cwd.

## Modules
- `packages/remote/src/shared/terminal/linkAffordance.ts` + `.test.ts` (~302 LOC)
- `packages/remote/src/shared/terminal/manager.ts` decideHoverUnderline / decideLinkClick / dataset.linkUnderline

## Evidence
`{SCRATCH}/gates-credit-C2.log` vitest linkAffordance exit 0

## Reframe
Full terminal rewrite → pure arbitration + hover underline slice.
