# Iteration 47 / AC4-C8 — Live backpressure vertical

## Shipped
- Rust `live_backpressure.rs`: per-session drop registry, aggregate snapshot, inject wiring
- Tauri `get_live_backpressure` desktop-only
- TS liveBackpressure + livePumpPolicy + hosts store pump product path + HostsPanel badges

## Gates
- cargo live_backpressure; vitest C8 suites → `{SCRATCH}/gates-credit-C8.log`

## Reframe
cgroup memory bar → application live buffer bound + operator badges
