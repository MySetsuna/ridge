# Iteration 14 报告 — M1 切片二 + M2：裁决审计持久化与 agent 归因（存量终轮）

日期：2026-07-23 · 合同：`CONTRACT-iteration-14.md`
分支：wind `codex/remote-git-diff-iteration-1`（Level 2 draft）

## 完成事项

1. **memory.rs 单源化**：新 `teammate/memory.rs` 承担 workspace-memory sidecar 的**唯一** doc 级读改写（进程互斥 RMW + 原子写 + `updatedAt` 元数据自动维护 + doc 空删文件）；`DIR` OnceLock 于 lib.rs setup 注入一次；suspend.rs 持久化改经 memory（跨节不互clobber），删旧 `state.app_handle` 双解析路径（`persist_for/load_all_for/remove_for` 签名减 state 参，六调用点同步）。
2. **decisions 落盘（M1 切片二）**：`PendingEntry` += `wid`；三消费点入审计——桌面 `resolve`（modify 只记动词不记替换文本）、远端 `resolve_remote`（consumed 与 nonce-mismatch 败者尝试皆记）、超时 fail-closed。条目 `{ts, source, initiator, verdict, riskLevel, reasonSummary, outcome}`，**绝不含命令全文**；环形上限 50。
3. **M2 归因**：route_send_keys 的 initiator 由 `pane#{idx}` 升级为稳定 `agent_id`（`teammate_agent_pane_map` 反查，未注册回落 pane 号）——审批与裁决审计归因到 agent 身份。
4. **读方**：桌面命令 `list_hitl_decisions`（仅本机 IPC，刻意不入 REMOTE_ALLOWLIST）；Agent Center 新「审批历史」区（最近 10 条：verdict 色标 + 摘要 + 归因 + 来源）。
5. 收窄注记：`already-resolved`/`bad-verdict` 尝试无条目可归属（wid 未知），不落审计——设计「败者尝试」记录范围为条目尚存的 nonce-mismatch 与消费成功者。

## 确定性验证证据（2026-07-23，全部 exit 0）

| 门禁 | 结果 |
| --- | --- |
| `cargo test --workspace` | exit 0 |
| `cargo test -p ridge --lib teammate::` | 25 passed（+memory RMW/cap/删空 +decision 落盘脱敏） |
| vitest `packages/remote/src/shared/` 全伞 | 559 passed / 1 skipped |
| `svelte-check --threshold error` | 71 files / 0 errors |

## 熔断记录

- 无停机条件触发。

## 用户必办件

核验会动线顺带项 +1：开 HITL → 触发并裁决（桌面或远端各一次）→ Agent Center 审批历史区出现条目（含 agent 归因）、重启后仍在。
