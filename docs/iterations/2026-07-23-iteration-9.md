# Iteration 9 报告 — G1 阶段一软暂停/恢复 + 审查闭环与簿记清偿

日期：2026-07-23 · 合同：`CONTRACT-iteration-9.md`
分支：wind `codex/remote-git-diff-iteration-1`（Level 2 draft）
本轮不变量兑现：**零 Remote 协议面变更**（allowlist/能力表/矩阵 methods 未动；`suspend_agent`/`resume_agent` 仅桌面 IPC，负断言入合同测试）。

## 完成事项

1. **G1[G1·主线] Agent 软暂停/恢复阶段一**：
   - 新模块 `src-tauri/src/teammate/suspend.rs`：进程级注册表（键 =（workspace, pane），类比 hitl.rs 不改 AppState）；`suspend/resume/is_suspended/clear_pane` 全幂等。
   - **Agent 写路径四所归一**：send-keys（HITL 网关后）、delegate 提示注入、MCP 文本注入、exec 命令注入全部改走 `suspend::agent_pty_write` 唯一收口——suspended → 明确「agent suspended」错误（send-keys 路由映射 403）；**桌面人类输入（`write_to_pty`）与断路器 Ctrl-C 刻意不门控**（接管语义 / 安全刹车）。
   - 清理三径：`release_teammate_agent_in`、`close_pane`、`remote_close_pane` 均调 `clear_pane`。
   - 状态投影：`topology_json` 与 `profiles::topology_for` 双路径 status 覆写 `Suspended`（无新字段，脱敏面不变——测试断言字段数仍 7）；TS `TeammateStatus` += 'Suspended'。
   - 桌面 UI：Agent Center 成员行悬停出 Pause/Play 按钮（title 注明「人类输入不受限」），状态点琥珀色。
   - 测试：门控拒绝/恢复放行（错误前缀区分门控 vs 透传）、幂等/键隔离/清理、拓扑 Suspended 投影与字段数钉死；`suspend_agent`/`resume_agent` 不可远达负断言。
2. **G2[C1] rdg 语义缺口报告**：`scripts/rdg-gap-report.mjs` 从 capability-matrix.json 派生 → `docs/audits/rdg-gap-report.md`（3 supported / 5 denied；teammate 标「刻意排除」，其余标「待人工判定」）。exit 0。
3. **G3[审查辅助] 分支审查导读包**：`scripts/generate-review-pack.mjs` → `docs/review/branch-review-guide.md`（36 提交自校验计数一致；5 协议面 / 7 安全面标注 + 按 type 分组 diffstat 表）。exit 0。
4. **G4[E1/E2] 簿记校正**：PROJECT-STATE §3.1 更正为「WebGPU-first + Canvas2D 回退，生产默认，不得删除」；E1 重定义为用户轨真机测量；E2 关闭待证据。NotebookLM 删除建议驳回已留档（guidance-8）。
5. **G5[A1] workspace 写路径审计**（零代码）：`docs/audits/workspace-write-paths.md`——create/save 已单源；switch/reorder/rename/close 桌面 vs 写端口为逐字双副本；**实分歧一处**：LAN host `close_workspace` 第三副本漏发 `WorkspacesChanged`/`WorkspaceListChanged` 双广播（LAN 关工作区不通知其他客户端）且多删 `workspace_names`。同源化候选顺序以 close 为首（升级为缺陷修复级）。

## 确定性验证证据（2026-07-23，全部 exit 0）

| 门禁 | 结果 |
| --- | --- |
| `cargo test --workspace` | exit 0 |
| `cargo test -p ridge --lib` | 96 passed（+3：suspend 门控/幂等、拓扑 Suspended 投影） |
| vitest `packages/remote/src/shared/` 全伞 | 559 passed / 1 skipped |
| `svelte-check --incremental --threshold error` | 0 errors |
| `node scripts/rdg-gap-report.mjs` | exit 0 |
| `node scripts/generate-review-pack.mjs` | exit 0（36 提交计数自校验过） |

## 熔断记录

- 无停机条件触发。G1 未发现绕过 send-keys 网关之外的第二 agent 写径需先收敛——审计确认四写点即全集（spawn-process 属进程注入面非 PTY 写，留待阶段二边界，报告如实记）。
- G5 审计发现 LAN close 副本实分歧——按合同「审计不动代码」仅记录，修复列入同源化候选首位。

## 用户必办件

不变四件 + Team 面板核验含 Pending approvals 区 + 新增：桌面开 teammate 后核验暂停/恢复按钮（暂停后 agent send-keys 被拒、人类可打字、状态点转琥珀）。
