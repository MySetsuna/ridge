# Iteration 42 / AC4-C3 — Git hard guards + shared process_guard

## What shipped
Git wall-clock timeout process-tree kill with observable counters; production spawn-only-via-git_cmd static gate; shared `process_guard` for tree kill; dual-end GIT_CONCURRENCY MIN/MAX cross-test; AgentCenter git guard badge.

## Modules
- `packages/ridge-core/src/process_guard.rs` (~182 LOC) + hung-binary test
- `packages/ridge-core/src/commands/git.rs` counters, static gate, kill→process_guard
- `src/lib/stores/gitGuardStats.ts` + tests; pLimit cross-end test

## Evidence
`{SCRATCH}/gates-credit-C3.log` process_guard + guard_tests exit 0
