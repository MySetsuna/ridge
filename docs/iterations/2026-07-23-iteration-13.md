# Iteration 13 报告 — P2 阶段 2 实现：远端 HITL 裁决通道

日期：2026-07-23 · 合同：`CONTRACT-iteration-13.md`
分支：wind `codex/remote-git-diff-iteration-1`（Level 2 draft）
**解冻存照**：维护态冻结由用户指令解除（「按你审理解继续推进」）；Level 2 与导读节律不变。

## 完成事项

1. **G1 Rust 核心**：`PendingEntry` += `nonce`（uuid v4 simple，122bit 不可猜，随挂起项同生共死）；`list_pending` 投影六字段（+`resolutionNonce`，**仍绝不含 action**）；`resolve_remote(id, nonce, verdict)`——同锁内查→恒时比对→一致才取出（单次消费原子）；verdict 仅 approve/reject（**modify 永不开放**）；结局四态 `consumed/already-resolved/nonce-mismatch/bad-verdict`；超时/已裁后到达无副作用；错 nonce 条目存活 + warn。桌面 `resolve`（含 modify）不变。
2. **G2 六处宣告**：`resolve_hitl_remote` 入 Rust/TS `REMOTE_ALLOWLIST` + `MUTATING_METHODS`（双侧同位，parity 测试自守）+ `REMOTE_CAPABILITY_METHODS.teammate` + LAN 路由臂 + tauri 注册 + 矩阵。合同测试：新增正断言（远端可达 ∧ 双侧 mutating）；负清单不减（桌面版 `resolve_hitl_request`/`set_hitl_enabled`/`suspend_agent`/`resume_agent` 仍不可远达）。
3. **G3 双实现 + UI**：`HitlPendingItem` += `resolutionNonce`；`resolveHitlRemote` LAN（invoke-request）/cloud（invoke）双实现；Team 面板 Pending 区 Approve/Reject 双按钮 + 非 consumed 结局一行反馈（下轮轮询消隐）；无二次确认框。

## 确定性验证证据（2026-07-23，全部 exit 0）

| 门禁 | 结果 |
| --- | --- |
| `cargo test --workspace` | exit 0 |
| `cargo test -p ridge --lib hitl` | 2 passed（单次消费/nonce/verdict 门 + 六字段脱敏） |
| vitest 合同/parity 定向 | 23 passed |
| vitest `packages/remote/src/shared/` 全伞 | 559 passed / 1 skipped |
| `svelte-check --threshold error` | 71 files / 0 errors |

## 熔断记录

- 无停机条件触发。

## 用户必办件

核验会动线追加一步：远端对一条待审批 Approve/Reject → 桌面侧命令相应放行/阻断；重放同 nonce → already-resolved。
