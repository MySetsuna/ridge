# Iteration 11 报告 — M1 切片一：暂停态持久化与启动恢复 + P2 阶段 2 设计

日期：2026-07-23 · 合同：`CONTRACT-iteration-11.md`
分支：wind `codex/remote-git-diff-iteration-1`（Level 2 draft）
红线兑现：零 Remote 协议面变更；sidecar 为已定设计的本地镜像。

## 完成事项

1. **G1[M1·主线] 切片一落地**：
   - `suspend.rs` 增持久层：`persist_to/load_from/remove_from`（dir 注入纯函数，单测友好）+ `persist_for/load_all_for/remove_for`（经 `state.app_handle` 解析 `{app_data}/workspace-memory/{wid}.json`）。本切片仅 `{suspendedPanes, updatedAt}`；原子写 temp+rename；空集删文件。
   - **fail-open 全程**：app 未就绪/IO 失败只 warn 不阻断；损坏 json 载入跳过（失忆非致命）；启动载入挂 lib.rs setup（app_handle 置后即载，无竞态——停机条款未触发）。
   - 写方钩全：`suspend_agent`/`resume_agent`（命令加 State 参）、`release_teammate_agent_in`、`close_pane`、`remote_close_pane` 变更后落盘；`close_workspace_core` 单点挂 `remove_for`（内存 + 文件同清，兑现 M1 设计的清理钩承诺）。
   - 测试：持久化→模拟重启（内存清）→载入恢复 `is_suspended` 真；resume 落盘空集删文件不复活；损坏 json 不 panic；关区清理内存文件同除（suspend 模块 3/3 绿）。
2. **G2[P2] 阶段 2 裁决通道设计定稿**（零代码）：一次性裁决票据（`resolutionNonce` 随挂起项生成，恒时比对、取出即毁）；**远端 modify 永不开放**（远程任意命令执行面）；120s fail-closed 不变不延、超时后裁决落空无副作用；多 controller 首达生效 + 败者入审计；审计接 M1 `decisions`（绝不存命令全文）；传输面选 `teammate` 能力新方法（对称阶段 1、走 A2 纪律，弃 0x12 CONTROL 混层案）；实现轮验收路径与「明确不做」清单齐备。
3. **G3[节律]**：全门禁维持绿；闭环前刷审查导读。

## 确定性验证证据（2026-07-23，全部 exit 0）

| 门禁 | 结果 |
| --- | --- |
| `cargo test --workspace` | exit 0 |
| `cargo test -p ridge --lib suspend` | 3 passed（sidecar 闭环/门控/幂等） |
| vitest `packages/remote/src/shared/` 全伞 | 559 passed / 1 skipped |
| `svelte-check --threshold error` | 71 files / 0 errors |

## 熔断记录

- 无停机条件触发：启动载入顺序无竞态（懒载降级条款未启用）。

## 用户必办件

不变四件；新增顺带核验：暂停一个 agent → 重启 Ridge → Agent Center 状态点仍琥珀、send-keys 仍被拒。
