# Iteration 40 / AC4-C1 — LAN Outbound PTY vertical

## What shipped (product)
Desktop multi-host controller can bind LAN outbound transport, list remote panes, subscribe, write/resize, pump live output into local parsers, and detach without killing remote PTY. Foreign close path in kill_pty_if_present + Hosts poll pump.

## Exclusive / primary modules
- `src-tauri/src/hosts/outbound.rs` (~516 LOC) Transport trait, Mock, wire mux, OutboundClient
- `src-tauri/src/hosts/lan_transport.rs` (~263 LOC) LAN phase machine + RPC backpressure
- `src-tauri/src/hosts/mod.rs` bind/list/attach/sink/pump/detach/live cap
- `src-tauri/src/commands/terminal.rs` foreign write/resize/kill detach
- `packages/remote/.../foreignPaneStatus.ts` UI state machine
- `src/lib/stores/hosts.ts` + HostsPanel product path

## Acceptance evidence
- Gate: `{SCRATCH}/gates-credit-C1.log` (`cargo test -p ridge --lib hosts::`) exit 0
- Key tests: pump_outbound_to_fanout_feeds_parser, hello_list_subscribe_write_resize_detach, lan_transport suite

## Reframe
Generic VNC / frontend multi-WebRTC → backend outbound + mock-deterministic path.

## Not counted as separate 2-day rounds
Former CONTRACT-22…24/29 labels (superseded).
