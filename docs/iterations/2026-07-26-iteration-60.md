# Iteration 60 报告 — 今日 NLM 对话需求全量落地

- **日期**: 2026-07-26
- **需求源**: 用户↔NLM 今日 10 问原文（`2026-07-26-nlm-conversations-today.md`，CDP 全量抓取）+ 会话中用户四次追加（G9/G10/G11/发布押后）。
- **对抗评审模式**: 放宽——不整案驳回，争议附替代案 AskUserQuestion 三问三答（品牌层改名 / 发现+编组 MVP / 搭 R-TESTGATE）。

## 评审核实（正确性门禁摘录）

| NLM 论断 | 核实结果 |
| --- | --- |
| 桌面 resize 失效因能力门拦截 | **证伪**：`resize_pane` 在 REMOTE_ALLOWLIST（remoteAllowlist.ts:54）。真因见 G2 |
| preferWebgpu 需改默认 | 已默认 true（manager.ts:643）；真回归是显示层丢绑定（G4） |
| 深改协议方法名 | 驳回替代：品牌层改名（用户裁定），wire 名不动零偏斜 |
| discovery 需新建模块 | 仓内已有休眠 discover.rs 纯匹配器+tauri 命令——接线而非新造 |

## 交付（按目标）

| ID | 状态 | 事实 |
| --- | --- | --- |
| G1 GIT-ABORT | ✅ | per-slot latest-win：`git_slot_begin/register/unregister` + thread-local 槽上下文进 `run_command_with_timeout`；同槽新请求 tree-kill 旧子进程、队列陈旧任务免 spawn 自弃、许可即时释放。前端 pill 双命令带 `pane:{id}:scm/diff` 槽 + 陈旧覆盖护栏。**4 项确定性单测**（真挂起二进制被取代杀掉/陈旧注册即杀/队列自弃/寄存器语义），`cargo test -p ridge-core --lib` 350 全绿 |
| G2 DESKTOP-FILL | ✅代码+待真机 | 根因：shared-remote（desktop-in-browser）attach/unpark **初次 fit 只 letterbox 从不 claim** → 永远显示 host 网格居中留白；按钮 claim 成功与否无验证。修：初次 fit 即 claim（仅 shared 模式，多观看者被动路径不变）+ `claimPaneSize` 1s 核对 kernel 网格、失配重试一次并 console.warn |
| G3 MOBILE-SIZE | 🔶部分 | 静态排查：unpark→fit→resizeHandler 链完整、self-heal 比对 kernel 在位；真机 CDP 复现见「运行时验证」节 |
| G4 WEBGPU | ✅ | 直接回归：P4 重构后 TerminalCanvas 从不回填 `backendName` → 页脚恒显 Canvas2D。修：manager 暴露 `backendName(paneId)`、attach 后回填；`_makeHandle` 回退警告去 DEV 门并报出「requested but got X」探测失败事实 |
| G5 COMMUNE-BRAND | ✅ | UI（指挥部头/侧栏 title）+ MCP serverInfo `agents-commune` / title `Agent's Commune`；wire 方法名不动 |
| G6 DISCOVERY | ✅ | 休眠 `discover_cli_agents` 接线：sysinfo 进程内枚举（替代裸 tasklist/ps spawn）+5s TTL 缓存；设置开关（默认开）；指挥部 Discovered 只读区。匹配器复用既有纯函数（原单测在位） |
| G7 COMMUNE-TOOLS | ✅ | 盘点：send/delegate/get_team_profile/join_group 四工具已在、groups 已随快照下发——缺口仅「队友在干什么」。补 roster 注入 pane `title`（typed/侧表两路径共用 `inject_roster_titles`） |
| G8 R-TESTGATE | ✅ | release.yml 新增 `test` job（vitest+svelte-check+ridge-core 单测+双 remote bundle 构建冒烟），`build.needs=test` 红即阻断 |
| G9 PANE-META-BCAST | ✅ | host 稳定聚合事件 `pane-meta-changed`；cloud 桥 `hostEventSource` tap（verified 门控/reset 退订）——cloud 腿此前**零事件推送**（pane-tree-changed 云上从未触发过，纯轮询）；控制端接 metaListeners；头部+Pane 弹层实时刷（弹层每 pane cwd 同步） |
| G10 NAV-BACK | ✅ | viewer 关闭改导航栈：回打开它的那级（记录 `viewerReturnTab`）；与旧「关文件回终端」诉求在栈语义下兼容 |
| G11 IME-DEDUP | ✅ | `imeCommitDelta` 纯函数（公共前缀去重+退格差量）**9 单测**含原例 `Spac+Space→e`；compositionend 与 insertReplacementText 双分支接线（后者由整帧丢弃改为差量应用，修正词不再丢失） |

## 顺手修（性能/护栏授权项）
- teammate/rollback.rs 裸 `Command::output` → process_guard 单出口（postmortem 规则 7 违例；超时杀树+CREATE_NO_WINDOW）。
- discover 原实现的裸 `tasklist`/`ps` spawn → sysinfo 进程内枚举（同一违例族）。
- paneGitStatus 陈旧响应覆盖新 cwd 状态的竞态护栏。
- `.gitignore` 补本机工具目录（.pnpm-store/.venv-notebooklm）。
- signaling vendor 与兄弟仓 re-sync（drift 门禁复绿）。

## 门禁状态
- `cargo test -p ridge-core --lib`：**350 passed**（含新 supersede 4 测）。
- `cargo check -p ridge`：绿（仅 2 条前存 unused-import 警告，非本轮引入）。
- `pnpm vitest run`：**1200 passed / 1 skipped**（含 imeDelta 9 新测；signaling drift 修复后全绿）。
- `pnpm check`（svelte-check）：**0 errors**（2 条前存 a11y 警告）。

## 运行时验证（tauri:dev:cdp，2026-07-26）

- **App 冒烟 PASS**：v0.1.3 debug 实例（与已装实例经隔离 profile 共存）正常起窗、建 pane；日志证据链：`create_pending → create_spawned → resize_ok rows=35 cols=138` —— 桌面 fit→resize_pane 主链在本轮 manager.ts 改动后运行无回归；建 pane 前的 `resize_fail (Pane not found)` 为既知良性竞态且自愈。
- **CDP in-app 自动化不可得（本机 WebView2 150）**：`WEBVIEW2_ADDITIONAL_BROWSER_ARGUMENTS` 的 `--remote-debugging-port=0` 未再进入浏览器进程 cmdline（150.0.4078.99；6-19 在 149 上曾验证可用）——`DevToolsActivePort` 不再生成。**按 contract 停机条款降级**：G2/G3 的 remote 腿交互证据（分屏拖拽跟随、手机视口尺寸一致性）标记「真机证据缺，转用户轨」；确定性信号以 Rust 350 测 + vitest 1200 测 + svelte-check 0 err + 上述 app 冒烟承担。
- **工具链发现（顺手记录）**：`tauri:dev:cdp` 首跑因 `ensure-teammate-shim` release 重建（4m12s）耗尽 tauri 180s 前端等待窗而退出；二跑（shim 已建）即过。CDP_TESTING.md 的 WebView2 150 失效待独立跟进（候选：改 `app.windows[].additionalBrowserArguments` 注入）。

## 开放问题 / 下轮候选
- R-INCR（gap 增量回放前端激活）、R-WSLEG、R-P4-LRU、R-P5P6 仍在 backlog（本轮边界）。
- G3 真机矩阵（iOS Safari / Android Chrome 实机）待用户轨补证。
- Commune 深层符号改名若仍有意愿，独立成轮（本轮品牌层已收）。
