# NotebookLM guidance — iteration 6 收口 → iteration 7 规划

生成时间：2026-07-23 16:30 +08:00 · 来源：唯一 `PROJECT-STATE`（source `46897810`）

## NotebookLM 原始指导（摘要）

1. Q1：**R1 弱网基准测量为主线**（> P2 只读展示 > G1 状态机设计）；验收 `artifacts/metrics-report.json` 含 TURN-only/5% 丢包/1s 延迟恢复指标。
2. Q2（P2 预案）：待审批快照走**新只读方法 `list_hitl_pending`**，不扩 teammate 载荷（最小暴露面 + 独立门控）。
3. Q3：**必须插入「冻结新功能、固化与文档」轮**——自动化已领先物理验证三个版本，「验证真空」在扩大。
4. Q4：**loader 载败不再挂起，必须修复**——已实质阻断新增 Rust 单测执行，威胁 T1。
5. 合同草案：R1 基准 + loader 修复 + T3 生产实跑 + 真机证据归档；减法 = Teammate 冗余字段清理。

## 对抗评审（checker：本地环境事实 + 合同纪律）

| 候选 | 裁决 | 关键理由 |
| --- | --- | --- |
| 冻结轮（Q3） | **采纳** | 与用户必办件积压事实一致；iteration 7 定为「证据与固化轮」，不加产品功能。 |
| loader 修复升为目标（Q4） | **采纳** | teammate 投影测试现「可编译不可执行」，T1 缺口实质化；停机条件保留（OS 级不可修 → 文档化 + 测试迁位）。 |
| R1「TURN-only/真实丢包」自动化验收 | **改写为双轨** | TURN-only、蜂窝、真实丢包需真实网络工装/凭据（Windows 无 netem，TURN 凭据在生产），自动轨无法诚实产出「真机结论」。自动轨改为：确定性弱网**实验室**参数化 harness（延迟/丢帧/背压参数扫描 + metrics JSON，显式标注实验室模型）；真实弱网仍归真机 runbook 用户轨。不以实验室数据冒充真机指标（沿用 iteration 4 反遥测/反模拟裁决）。 |
| T3 生产实跑、真机证据归档入合同 | **改列用户轨** | 凭据与设备在用户手中，不能作为自动轨可验收目标；合同分「自动轨/用户轨」双列。 |
| 减法「清理 Teammate 冗余字段」 | **收窄为审计先行** | 无预置证据；先 rustc/grep 双证审计，确有无消费字段才删（沿 A1 纪律）。 |
| Q2 `list_hitl_pending` 方案 | **归档待用** | P2 冻结中；方案要点（独立只读方法 + 独立门控 + A2 宣告）记入合同「明确不做/未来」。 |

## 经检验后的 iteration 7 方向

证据与固化轮，双轨制：自动轨 = loader 修复（主线）+ 弱网实验室 harness + Teammate 字段审计 + 用户必办件清单/文档固化；用户轨 = 真机 smoke、生产实跑、两分支合并、Team 面板核验。见 `CONTRACT-iteration-7.md`。
