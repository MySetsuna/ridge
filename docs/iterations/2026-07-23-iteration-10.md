# Iteration 10 报告 — A1 写路径同源化 + LAN 漏广播修复 + M1 设计与簿记清偿

日期：2026-07-23 · 合同：`CONTRACT-iteration-10.md`
分支：wind `codex/remote-git-diff-iteration-1`（Level 2 draft）
不变量兑现：零 Remote 协议面变更；M1 止于设计（零代码）。

## 完成事项

1. **G1[A1·主线] close/rename 三副本同源化 + 缺陷修复**：
   - `close_workspace_core`：三调用方（桌面命令 / `WorkspaceWriter` 端口 / LAN `remote_host_impl`）全部委托唯一实现——**LAN 副本漏发 `WorkspacesChanged`/`WorkspaceListChanged` 的实缺陷随收敛修复**（LAN 关区现通知他端）；`workspace_names` 清理三方对齐（旧桌面/端口副本残条泄漏一并修）。
   - `rename_workspace_core`：双副本收敛（改名 + auto-save + 三广播链）。
   - 净减 ~80 行重复实现。行为注记：LAN close 错误文案由英文改为唯一实现的中文（无合同钉死，导读留痕）。
   - 测试：订阅 `remote_structural_tx` 断言 close 发 `WorkspacesChanged`、rename 发 `WorkspaceRenamed`+`WorkspacesChanged`；names 清理与最后一个守卫钉死（2 测全绿）。
2. **G2[M1] Workspace Memory 设计文档**（零代码）：6 字段语义 + 首个读写方表；落点选型 **B（独立 sidecar json）**（与 `.ridge` 布局解耦：写频/生命周期/损坏域不同；清理挂已同源化的 `close_workspace_core` 单点）；隐私边界（decisions 只存风险分类+摘要，绝不存命令全文）；三切片实施序（切片一 = suspended panes 持久化 + 启动恢复）。驳回 struct-only 三度提案的替代产物。
3. **G3[H1] 簿记降级**：H1 改「已关闭——待用户真实需求证据重开」（类 E2）；`hosts` 死变体容忍保留注记。
4. **G4[C1] 逐缺口判定入脚本**：`JUDGMENTS` 静态表（teammate=刻意排除；theme/invoke=语义完备的永久缺口；git/workspace=补路由候选待需求）；报告再生成，**零「待人工判定」残留**。
5. **G5[节律] 固化**：WORKFLOW.md 补「每轮闭环必刷审查导读 + 积压期不扩协议面」；本轮闭环执行。

## 确定性验证证据（2026-07-23，全部 exit 0）

| 门禁 | 结果 |
| --- | --- |
| `cargo test --workspace` | exit 0 |
| `cargo test -p ridge --lib workspace::tests` | 2 passed（broadcast/names/guard 钉死） |
| vitest `packages/remote/src/shared/` 全伞 | 559 passed / 1 skipped（37 文件） |
| `svelte-check --threshold error` | 71 files / 0 errors |
| `node scripts/rdg-gap-report.mjs` | exit 0，判定零残留 |
| `node scripts/generate-review-pack.mjs` | exit 0（闭环前刷新） |

## 熔断记录

- 无停机条件触发：同源化未暴露锁序冲突（三副本本就同序）；rename 未触发「仅收敛 close」减法条款。

## 用户必办件

不变四件（导读已备且本轮再刷新）+ LAN 关区广播修复可顺带核验（两客户端连 LAN，一端关区另一端列表即更新）。
