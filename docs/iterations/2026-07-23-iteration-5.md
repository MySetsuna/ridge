# Iteration 5 报告 — 可信基线固化（S1 审计 + T3 状态证据 + T1 绿灯 + A2 矩阵 + A1 减法）

日期：2026-07-23 · 合同：`CONTRACT-iteration-5.md`
分支：wind `codex/remote-git-diff-iteration-1` @ `292adb3`；ridge-cloud `codex/remote-artifacts-status` @ `7a079bb`（均 Level 2 draft）

## 完成事项

1. **G1[S1] 安全回落审计**（commit `8f65c59`）：
   - `docs/security/cloud-fallback-matrix.md`：5 构造点 × 5 校验器矩阵 + 回落面 F1–F6 清单（含退役条件），全部 `file:line` 证据。
   - 3 条钉死测试入 `cloudHostBridge.test.ts`（56/56 绿）：bind-only 无降级、**无 transcript 时 trust-proof 退化为无信道绑定 + 信任库裁决**（审计发现：与源注释相悖，注释已更正）、transcript 不对称即拒。
   - 遥测/退役设计 `2026-07-23-s1-fallback-telemetry-retirement-design.md`（零行为变更）。
2. **G2[T3] 生产两线状态证据**：
   - ridge-cloud（`7a079bb`）：`activate()` 写 `current.json` 标记；新增 token 守卫只读 `GET /api/v1/remote-artifacts/status`（current 版本 + releases + index 在位）；+3 测试，cargo 121→124 全绿。
   - wind（`aedb8e0`）：`scripts/check-prod-status.mjs` 一键两线汇总；本地桩验证通过/401/缺 token（显式「未验证」）/网络失败四径。
3. **G3[T1] 双仓 cargo**：wind `--workspace --exclude ridge` 882 绿 + `-p ridge --bins` 27 绿；ridge-cloud 124 绿。**`-p ridge --lib` 测试宿主载败**（见熔断）。
4. **G4[A2] 能力矩阵机器可读化**（`c8f7ead`）：`docs/capability-matrix.json`（7 能力 × 6 入口）+ 6 条一致性测试与 canonical 声明互证（13/13 绿），rdgHost 列由 `CLI_CAPABILITIES` 推导（pane/fs/search supported，余 denied）。
5. **G5[A1] 减法**（`b9b4d4f`）：审计报告 `2026-07-23-a1-subtraction-audit.md`；示范删除 state.rs 死 pane-output 通道面（类型+字段+三方法，净 −45 行，rustc+grep 双证）；确认 git 面已薄委托、workspace 只读面是真双路径（下切片候选）。
6. **计划外修复**（`292adb3`）：signaling drift 门禁 Windows 误报——autocrlf 把 vendored 副本涂成 CRLF，破坏与 LF 钉死的同级仓的字节一致；新增 `.gitattributes` 钉 LF + 工作树还原，门禁复绿。

## 确定性验证证据（2026-07-23，除注明外 exit 0）

| 门禁 | 结果 |
| --- | --- |
| wind `cargo test --workspace --exclude ridge` | 882 passed（7 成员全绿） |
| wind `cargo test -p ridge --bins` | 27 passed |
| wind `cargo test -p ridge --lib` | **红**：`STATUS_ENTRYPOINT_NOT_FOUND (0xc0000139)`，测试宿主启动即败 |
| ridge-cloud `cargo test` | 124 passed |
| vitest cloud/ + transport/ 全伞 | 382 passed / 1 skipped |
| `svelte-check --incremental --threshold error` | 70 files / 0 errors |
| `check-prod-status.mjs` 桩验 | 通过/401/未验证/网络失败四径符合预期 |

## 熔断记录

- **`-p ridge --lib` 载败**：loader 级（进程启动前），静态导入全系统 DLL 且 ldd 可解析；同机 ridge-core/ridge-term/bins 测试宿主均正常。首因先于本合同（今日无 Rust 行为改动即已败）。按停机条款如实记录不宣称绿；根因定位（疑 Tauri 静态链接面 + 系统 DLL 符号快照）列开放问题，未阻塞其余目标。
- 生产实跑（T3 脚本对真实域名 + token）在用户手中，与真机 smoke（R1）并列为用户必办件。

## 用户必办件（不变 + 新增）

1. 真机双平台 smoke（runbook 已备，R1）。
2. `check-prod-status.mjs --base-url <生产域名>` + `RIDGE_ARTIFACT_TOKEN` 实跑留证（T3 收口）。
3. ridge-cloud `codex/remote-artifacts-status` 分支审查合并 + 部署（含 status 端点上线）。
