# Iteration 6 报告 — P1 Remote Agent 控制台 MVP + S1 计数 + A1 同源化

日期：2026-07-23 · 合同：`CONTRACT-iteration-6.md`
分支：wind `codex/remote-git-diff-iteration-1` @ `5eca329`（Level 2 draft）

## 完成事项

1. **G1[P1] teammate roster 跨入口只读可见**（commit `72ef6dd`）：
   - 新协商能力 `teammate`，唯一方法 `get_teammate_topology`（只读、UI 轮询 5s，不建订阅流）。
   - 六处同步宣告：`REMOTE_ALLOWLIST`（Rust + TS 镜像）、`REMOTE_CAPABILITY_METHODS`、`CLIENT_CAPABILITIES`、LAN `HOST_CAPABILITIES`、cloud `HOST_CAPABILITIES`、`capability-matrix.json`（rdgHost=denied、rdgController=not-applicable）；`CLI_CAPABILITIES` 刻意不增列。
   - 路由：LAN host 走 `dispatch_invoke_request` 显式白名单臂；cloud 走 tauriShim invoke → bridge allowlist 门控；`RemoteLink` 双实现（LAN `invoke-request` 信封 / cloud invoke）各补 `getTeammateTopology`。
   - UI：共享 controller（桌面+移动同码）新增 `SidebarTeamRoster`（状态点、Leader 冠标、点按切 pane、无团队零噪声）；MainApp/RemoteSidebar 均按 `{#if …team}` 能力协商模式接入。
   - 安全：Rust 测试钉死投影仅 `id/name/paneId/paneIndex/role/status/capability`（无 token/endpoint/env/mcp 字样）；合同测试断言 `resolve_hitl_request`/`set_hitl_enabled` 仍不可远达。
2. **G2[S1] F1/F2 回落计数**（commit `db7f3b2`）：bridge `fallbackCounters`（trust-proof transcript 在/缺，缺时一行诊断）；双 provider `bindingCounters`（enforced vs relay-trust）。进程内计数 + 既有测例钉死恰好 +1，无新持久面。
3. **G3[A1] workspace 列表同源化**（commit `5eca329`）：`list_workspaces_entries` 单一实现喂桌面 IPC 命令与 remote 端口；删平行结构 `WorkspaceInfo`（序列形不变，net −12 行）。`get_active_workspace_id`/`get_workspace_snapshot` 审计确认本已单源，无需改。

## 确定性验证证据（2026-07-23，全部 exit 0 除注明）

| 门禁 | 结果 |
| --- | --- |
| vitest `packages/remote/src/shared/` 全伞 | 558 passed / 1 skipped |
| `svelte-check --incremental --threshold error` | 71 files / 0 errors |
| `cargo check -p ridge` + `cargo test -p ridge --lib --no-run` | 0 errors（测试宿主可编译） |
| `cargo test -p ridge --bins` | 27 passed |
| `cargo test --workspace --exclude ridge` | 882 passed / 0 failed |
| `cargo test -p ridge --lib` 执行 | 仍阻于既知 loader 载败（环境债务，Q4 挂起） |

## 熔断记录

- 无停机条件触发：拓扑投影脱敏断言一次通过；矩阵一致性测试与 canonical 声明零冲突；G3 无形状不兼容。
- 一处预期红→绿：cloudHostBridge「advertises full host capabilities」硬编码七能力清单，加 `teammate` 后按合同更新为八项。

## 用户必办件（不变）

真机双平台 smoke（R1）；生产 status 实跑（T3 收口）；wind 与 ridge-cloud 两分支审查合并。
新增建议核验：桌面/手机连一次 Remote，确认 Team 标签出现、roster 可见、点按切 pane 生效（人工 UI 验收）。
