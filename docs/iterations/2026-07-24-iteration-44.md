# Iteration 44 / AC4-C5 — Outbound reconnect supervisor (NEW)

## What shipped
Cancelable multi-host reconnect state machine: schedule/step/cancel; disconnect schedules attached panes; Tauri `step_host_reconnect` / `cancel_host_reconnect`; desktop `hostReconnect` store + pure message parse tests.

## Modules
- `src-tauri/src/hosts/reconnect_supervisor.rs` (~295 LOC) + 5 unit tests
- `hosts/mod.rs` on_host_disconnected_schedule_reconnect
- `src/lib/stores/hostReconnect.ts` + `.test.ts`

## Evidence
`{SCRATCH}/gates-credit-C5.log` reconnect_supervisor + hostReconnect vitest exit 0
