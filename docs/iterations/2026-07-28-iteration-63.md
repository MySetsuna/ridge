# Iteration 63 · Mobile Remote 连续性与 active QoS

日期：2026-07-28
状态：代码关闭，待发布
需求：`REQ-MOBILE-REMOTE-STATE-01` v0.2.2
合同：`docs/iterations/CONTRACT-iteration-63.md`
提交：`9bbfb5e`

## 结果

- Remote 服务器状态迁入 Svelte Query；UI 状态留本地 store；PTY bytes/kernel 不进 cache。
- visited pane 跨 workspace 保活；普通切换不退订、不清 kernel、不全量重放。
- LAN/Cloud 共用 active/background 逻辑优先语义；后台有界丢弃后按 pane dirty，一次 canonical recovery。
- 软键盘仅作有界视觉位移，不改变 terminal/canvas/grid/PTY 尺寸。
- scrollback 以 seq 邻接校验并原子提交；加载光条不占布局。
- Agent/Shell 触发器改纯 icon；补齐 LAN `git_stash_list` 两路远程允许。
- `remote-dist` 仍仅为统一生成产物，不存在 `web-remote-dist` 分叉。

## 验证

| 闸 | 结果 |
| --- | --- |
| `pnpm test` | exit 0；108 files，1257 passed，1 skipped |
| `pnpm check` | exit 0；0 errors，2 个既有 a11y warnings |
| `cargo check --manifest-path src-tauri/Cargo.toml --lib` | 编译完成；38 个既有 warnings |
| `cargo test --manifest-path src-tauri/Cargo.toml --lib` | 189 passed，0 failed |
| `pnpm build:remote` | exit 0；desktop + mobile production bundles |
| 最新嵌入产物隔离 Ridge host + `scripts/remote-state-e2e.mjs` | exit 0 |

真 LAN E2E 以新浏览器上下文、一次性配对码和真实 PTY 执行，断言：

- `ws1/A → ws1/B → ws2/C → ws1/A`；A/B/C 后台持续收流。
- 普通切换无 unsubscribe，无 full RIS；回 A 立见尾帧。
- 软键盘等价 visual viewport fixture 下 container/canvas 尺寸不变，transform 有限且关闭后复原。
- 真实 PTY 生成超 500 KiB 历史；滚顶显示 loading；请求/结果 seq 严格相邻；提交后 loading 清除。
- 桌面 Remote 实际发出 `git_stash_list`，host 正常返回，无 `RpcRemoteError`。

## 边界

- 此轮有真实 LAN 浏览器/PTY 证据；公网生产 WebRTC/E2EE 仍须部署后另验，不据 LAN 结果宣称已验证。
- Query cache 不持有 PTY bytes、terminal kernel 或 DOM。
- active 优先为同一物理链路内逻辑保留通道，不承诺突破物理带宽。

## 下一项

`REQ-AGENT-HISTORY-01` 已获用户自动批准；须在 NotebookLM 来源替换后规划并对抗评审，形成 iteration 64 合同。
