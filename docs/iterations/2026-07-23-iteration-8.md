# Iteration 8 报告 — P2 阶段 1：HITL 待审批远端只读可见 + G1 设计 + A1/S1 支线

日期：2026-07-23 · 合同：`CONTRACT-iteration-8.md`
分支：wind `codex/remote-git-diff-iteration-1`（Level 2 draft）

## 完成事项

1. **G1[P2·主线] `list_hitl_pending` 只读端到端**：
   - `hitl.rs` PENDING 注册表加宽：`PendingEntry {tx, initiator, reason, created_at_ms}`（原先元数据发事件即弃，NotebookLM 未见此前提，对抗评审补正后入合同）；`list_pending()` 产出**脱敏**投影 `{id, initiator, level:"Dangerous", reason, createdAt}`，按 createdAt 稳定排序；resolve/超时清理路径不变。
   - 六处宣告同步：Rust `REMOTE_ALLOWLIST` + TS 镜像（逐项 parity 测试守卫）+ `REMOTE_CAPABILITY_METHODS.teammate` + LAN `invoke-request` 路由臂 + tauri command 注册 + `capability-matrix.json` teammate.methods。
   - **脱敏钉死**（安全关键）：Rust 测试断言投影字段恰为五者、**绝无 `action`**（命令全文可含密钥；全文只随桌面事件走本机）；`resolve_hitl_request`/`set_hitl_enabled` 不可远达负断言维持。
   - UI：Team 面板顶部「Pending approvals」只读区（ShieldAlert + reason + initiator；空则零噪声）；同 5s 轮询；无任何裁决按钮。
2. **G2[G1] 暂停/恢复设计文档**（零代码）：`docs/superpowers/specs/2026-07-23-agent-suspend-resume-design.md`——三层可暂停边界（会话/输入/进程）；Windows 无 SIGSTOP 的三候选对比（NtSuspendProcess 树遍历过渡 → Job Object 冻结目标态，L-b 输入门控兜底）；Unix SIGSTOP/SIGCONT 进程组；先关输入再冻结的顺序不变量；HITL 120s fail-closed 计时不暂停；fail-visible（冻结成功才标记，半失败回滚）。按合同驳回 NotebookLM「先加 Suspended/Resuming 枚举变体」（无实现的死状态）。
3. **G3[A1] pane.rs 分类审计**：12 公开命令 + 10 pub(crate) helper 全量消费核查（读写分类见下）；`register/release_teammate_agent_in` 零外部调用但为公开命令的 T5 可测性内核（活）。rustc dead_code 扫出**真死码二处并删除**：`pane_tree.rs::first_leaf/last_leaf`（仅自递归，零消费）与 `parser.rs::full_reframe_with_scrollback`（§5 移动端 per-client parser 方案遗物，零消费），net −60 行。
4. **G4[S1] F3/F4 计数 + F5 退役删除**：
   - F3：controller `bindingCounters.tofuChanged`（TOFU 指纹变化 warn-only 路径）；新测试以合法 0x02 + 预 pin 不同指纹驱动，断言计数 +1 且仍 enforced connected。
   - F4：host `bindingCounters.fallback0x01`（无身份密钥 + 签名失败降级两路径）；既有 0x01 回落测试补计数断言。
   - F5：`keyBindingVerifier` 钩子**删除**（生产零接线双证：唯一构造点未注入、`verifyPeerKey` 无生产调用方；目标场景由 0x02+B3 覆盖）——删桥 config/字段/方法、`makeKeyBindingVerifier`、9 条对应测试；矩阵 F5 行改「已退役（删除）」。
   - 计划外根修：`deviceTrust.localStorageTrustStore` 在 Node/受限环境下 `localStorage` 为不可写残缺对象（setItem 即抛）且内存回退为每调用新 Map（跨调用失忆）——改探针验证 + 模块级共享内存回退。

## pane.rs 读写分类（G3 审计产物）

- 只读：`get_pane_layout(_for)(_inner)`、`teammate_pane_uuid_at_index`、`teammate_pane_is_leaf`、`choose_balanced_split`（纯决策）。
- 写：`split_pane(_inner)`、`set_split_ratios_at_path/_batch`、`dock_pane`、`close_pane`、`toggle_mode(_inner)`、`register/release_teammate_agent(_in)`、`teammate_split_pane`、`remote_create_pane`、`remote_close_pane`。
- 写路径同源化按合同明确不做（真机证据空窗期）。

## 确定性验证证据（2026-07-23，全部 exit 0）

| 门禁 | 结果 |
| --- | --- |
| `cargo test --workspace` | exit 0（含 ridge-cli 113、ridge-core 324、ridge-term 380 等全绿；死码删除与 hitl 扩表连带验证） |
| vitest `packages/remote/src/shared/` 全伞 | 559 passed / 1 skipped（567−9 删除测试+1 新 TOFU 测试，帐目符） |
| vitest cloud 子伞 | 236 passed / 1 skipped |
| `svelte-check --incremental --threshold error` | 0 errors |
| `cargo test -p ridge --lib` | 93 passed（+1 hitl 脱敏测试） |

## 熔断记录

- 无停机条件触发。G1 脱敏投影一次成型；F5 删除后全伞绿（未触发回滚降级条款）。
- 发现并根修一处环境级测试地雷（Node localStorage 残缺对象），属 G4 实施途中的必要修复，非范围外扩张。

## 用户必办件（见 `docs/plans/user-verification-checklist.md`，不变四件 + 新增一项核验）

真机双平台 smoke；生产 status 实跑；两分支审查合并；Team 面板人工核验（现含 Pending approvals 区：开 HITL 网关后触发一条高危命令，确认远端只见风险摘要不见命令全文）。
