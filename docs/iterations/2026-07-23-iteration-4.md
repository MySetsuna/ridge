# Iteration 4 报告 — Cloud Remote 双平台真机生命周期 smoke（自动前置收口）

日期：2026-07-23（合同签发 2026-07-21，本日收口）
合同：`CONTRACT-iteration-4.md`
分支：`codex/remote-git-diff-iteration-1` @ `6be9173`（较 `origin/main` 领先 13 提交，Level 2 draft）

## 完成事项

1. **短自动前置**（commit `d029a65`）：`faultInjection.test.ts` 新增两条生产类时序：
   - `disconnected <15s` 自愈：无 ICE restart offer、无 PC/WS 重建、无重复 hello/pane recovery、timer 清零；
   - `watchdog 15s → ICE restart → deadline 12s` 未恢复：升级整体重建，旧 PC/DC/WS 关闭，新连接重新 E2EE + TOTP 后恢复恰好一次。
   未新增 watchdog/deadline 实现（已存在于 `ControllerCloudProvider`），仅补门禁。
2. **物理 smoke 证据套件**（commits `20b48f6`、`6be9173`）：
   - 设计文档 `docs/specs/2026-07-21-cloud-remote-physical-smoke-design.md`；
   - 聚焦 runbook `docs/plans/cloud-remote-physical-smoke-runbook.md`（不复用旧过宽 checklist）；
   - evidence JSON Schema + 示例 + 校验脚本 `scripts/validate-remote-smoke-evidence.mjs`；
   - 证据目录 `/artifacts/remote-smoke/` 已 gitignore，模板无 JWT/TOTP/账号/完整设备域名。

## 确定性验证证据（2026-07-23 运行，全部 exit 0）

| 门禁 | 结果 |
| --- | --- |
| `vitest run …/faultInjection.test.ts` | 7/7 passed（含两条新时序） |
| `vitest run` Cloud 定向 5 文件（provider/bridge/adapter/rpcClient/conformance） | 156/156 passed |
| `svelte-check --incremental --threshold error` | 70 files / 0 errors |
| `node scripts/validate-remote-smoke-evidence.mjs` 对示例 | valid, exit 0 |

## 未完成（顺延）

- **真机双平台人工验收**：`artifacts/remote-smoke/` 为空。iOS Safari 与 Android Chrome 的基线连接、Wi-Fi→蜂窝→Wi-Fi、后台跨 15 分钟 token 窗口、恢复 ≤45s 判定，须由人持真机按 runbook 执行并产出过 schema 校验的 evidence JSON。**不以自动测试或单平台结果宣称双平台通过。**此项为当前唯一用户必办件，顺延至用户择机执行，不阻塞后续自动化迭代。

## 熔断记录

- 无停机条件触发：自动前置全绿、无可复现产品缺陷暴露、未出现要求新增 RPC/后端存储/遥测的证据采集需求。
- 本轮为「收口轮」：开发工作在 2026-07-21 已提交，本日补跑验收、更新状态文档并完成 NotebookLM 往返（此前遗留）。

## 存量来源压缩（本日一并执行，经用户指示「轻量多余来源」授权）

- 笔记本原 7 来源（PROJECT_STATUS / PRODUCT_VISION / GAP_PORTFOLIO / iteration 1–3 报告 / iteration 2 checker 附录）全文已导出归档至 `docs/ridge-baseline-digest/*.source.md`（iteration 报告本地已有，不重复导出）。
- 三份基线文档骨架与结论合并改写进 `docs/PROJECT-STATE.md`（新唯一来源），差距表按 iteration 1–4 结果更新状态（T2 关闭、A2 主体关闭、R1 实验室关闭等）。
- 上传 `PROJECT-STATE.md` 并确认入库后，删除全部旧来源，终态 1 来源。
