# Ridge 0.0.18

## Highlights
- Open vision checklist closed (iteration 15–16): hosts TCP + live input routing, Job Object on PTY spawn, workspace memory goal/constraints/tasks, REMOTE_UI_MISSING, mobile copy, agent discovery, git rollback, OS freeze hooks.
- Skill: Notes cleared only when visions are code-implemented; `[已实现]` counts as cleared.

## Remote cloud artifacts
- Not published from this machine (no RIDGE_ARTIFACT_TOKEN). On the machine with the token:
  `RIDGE_CLOUD_ARTIFACT_URL=... RIDGE_ARTIFACT_TOKEN=... pnpm publish:remote-cloud`

## Level 2
- Branch remains draft; no auto-merge.
